# Copy this file to `.env.development` and set your ngrok HTTPS URL
# Example:
# REACT_APP_API_URL=https://your-ngrok-subdomain.ngrok-free.dev

# If unset, the frontend will use relative API paths like `/api/...` (calls the current origin).
REACT_APP_API_URL=
